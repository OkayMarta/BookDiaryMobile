<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/button_apply_sort">

        <RadioGroup
            android:id="@+id/radio_group_sort"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView style="?attr/textAppearanceTitleMedium" android:text="Sort by Date" android:layout_marginBottom="8dp"/>
            <RadioButton android:id="@+id/radio_date_desc" android:text="Date (Newest first)" />
            <RadioButton android:id="@+id/radio_date_asc" android:text="Date (Oldest first)" />

            <com.google.android.material.divider.MaterialDivider android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginVertical="16dp"/>

            <TextView style="?attr/textAppearanceTitleMedium" android:text="Sort by Title" android:layout_marginBottom="8dp"/>
            <RadioButton android:id="@+id/radio_title_asc" android:text="Title (A-Z)" />
            <RadioButton android:id="@+id/radio_title_desc" android:text="Title (Z-A)" />

            <com.google.android.material.divider.MaterialDivider android:id="@+id/divider_rating" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginVertical="16dp"/>

            <TextView style="?attr/textAppearanceTitleMedium" android:id="@+id/label_rating" android:text="Sort by Rating" android:layout_marginBottom="8dp"/>
            <RadioButton android:id="@+id/radio_rating_desc" android:text="Rating (Highest first)" />
            <RadioButton android:id="@+id/radio_rating_asc" android:text="Rating (Lowest first)" />

        </RadioGroup>
    </ScrollView>

    <Button
        android:id="@+id/button_apply_sort"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Apply"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>