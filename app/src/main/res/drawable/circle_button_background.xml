<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Колір хвилі при натисненні -->

    <!-- Маска, яка обмежує поширення хвилі.
         В нашому випадку це коло. -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>

    <!-- Фоновий Drawable, який буде видимий у звичайному стані.
         Це оригінальна фігура з рамкою. -->
    <item>
        <shape android:shape="oval">
            <!-- Прозорий фон -->
            <solid android:color="@android:color/transparent" />
            <!-- Біла рамка -->
            <stroke
                android:width="1dp"
                android:color="@color/text_primary" />
        </shape>
    </item>

</ripple>