<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Вигляд, коли кнопка "вибрана" (isSelected="true") -->
    <item android:drawable="@drawable/circle_button_background_selected" android:state_selected="true" />

    <!-- Вигляд за замовчуванням (звичайний стан) -->
    <item android:drawable="@drawable/circle_button_background" />

</selector>